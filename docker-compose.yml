services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    cap_add:
      - NET_RAW 
    network_mode: "host"

  frontend:
    build: ./frontend
    ports:
      - "5173:80" 
    depends_on:
      - backend
    network_mode: "host"
